#include <stdio.h>

/**
 * main - prints program name, followed by a new line
 * @argc: int
 * @argv: list
 * Return: 0 Always.
 */
int main(int argc, char *argv[])
{
	(void)argc;

	printf("%s\n", argv[]);
	return (0);
}
